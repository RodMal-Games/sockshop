apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: carts-green
  labels:
    app: {{ .Chart.Name }}-selector-carts
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  replicas: {{ .Values.cartsGreen.replicaCount }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-selector-carts
        deployment: carts-green
    spec:
      containers:
      - name: {{ .Values.cartsGreen.container.name }}
        image: "{{ .Values.cartsGreen.image.repository }}:{{ .Values.cartsGreen.image.tag }}"
        imagePullPolicy: {{ .Values.cartsGreen.image.pullPolicy }}
        env:
        - name: DT_TAGS
          value: "application={{ .Chart.Name }}"
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.name"
        - name: DEPLOYMENT_NAME
          valueFrom:
            fieldRef:
              fieldPath: "metadata.labels['deployment']"
        - name: CONTAINER_IMAGE
          value: "{{ .Values.cartsGreen.image.repository }}:{{ .Values.cartsGreen.image.tag }}"
        - name: KEPTN_PROJECT
          value: "{{ .Chart.Name }}"
        - name: KEPTN_STAGE
          valueFrom:
            fieldRef:
              fieldPath: "metadata.namespace"
        - name: KEPTN_SERVICE
          value: "{{ .Values.cartsGreen.service.name }}"
        ports:
        - name: internalport
          containerPort: {{ .Values.cartsGreen.service.internalPort }}
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 15
        resources:
{{ toYaml .Values.resources | indent 12 }}
